export const GATEWAY = {
  protocol: "http",
  host: "localhost",
  port: 80,
  apiPrefix: "",
};

export const DASHBOARD = {
  protocol: "http",
  host: "localhost",
  port: 3000,
  apiPrefix: "/d",
};
